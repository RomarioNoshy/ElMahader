import { ConvertToArabicPipe } from './convert-to-arabic.pipe';

describe('ConvertToArabicPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToArabicPipe();
    expect(pipe).toBeTruthy();
  });
});
